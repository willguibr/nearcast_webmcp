{
  "name": "nearcast-webmcp",
  "private": true,
  "version": "0.1.0",
  "description": "Nearcast WebMCP — cross-border hazard awareness with WebMCP tools for AI agents",
  "license": "MIT",
  "workspaces": ["shared", "backend", "frontend"],
  "scripts": {
    "build": "npm run build -w backend && npm run build -w frontend",
    "test": "npm run test -w backend && npm run test -w frontend",
    "typecheck": "npm run typecheck -w shared && npm run typecheck -w backend && npm run typecheck -w frontend",
    "dev": "npm run dev -w frontend",
    "dev:api": "npm run dev -w backend",
    "deploy": "bash scripts/deploy.sh"
  },
  "engines": { "node": ">=20" }
}
