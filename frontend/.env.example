# Set to "true" to load synthetic demo hazards instead of calling /api (clearly labelled DEMO DATA in the UI).
VITE_USE_MOCK_DATA=false
# Local dev only: where Vite proxies /api/* (backend dev server or a deployed CloudFront URL).
VITE_API_PROXY_TARGET=http://localhost:8787
